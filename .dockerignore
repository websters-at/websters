.git
.gitignore
README.md
docker-compose.override.yaml
node_modules
Dockerfile
docker-compose.yml
.dockerignore
docker/
tests/
storage/logs/
storage/framework/cache/
storage/framework/sessions/
storage/framework/views/

# Bind-mount data dirs must NOT enter the build context
caddy/
mysql/
redis/
# NOTE: .env IS intentionally included in the build context because the
# entrypoint runs `php artisan config:cache` at container start. It is
# gitignored, so it never enters the git repository.
